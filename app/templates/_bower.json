{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {
    <% if (requireJS) { %>
      "requirejs": "~2.1.9",
      "almond": "latest",
    <% } 
    if (includeBootstrap) { 
        if (includeCompass) { %>
    "sass-bootstrap": "~3.0.0",
        <% } else { %>
    "bootstrap": "~3.0.3",
    <% }} if (includeModernizr) { %>
      "modernizr": "~2.7.1",
    <% } %>
    "react": "~0.8.0",
    "jquery": "~1.11.0"
  },
  "devDependencies": {}
}
